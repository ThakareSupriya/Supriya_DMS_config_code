package webCommonFunctions;

import org.openqa.selenium.WebElement;

public class WebButton {
	/* Method to click on the web buttons
	 * Parameters -  WebElement
	 */
	public static void clickButton(WebElement element ) {
				element.click();
			}
}
